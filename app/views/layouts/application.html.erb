<!DOCTYPE html>
<html>
  <head>
    <title>TheGossipProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link href="https://stackpath.bootstrapcdn.com/bootswatch/4.3.1/litera/bootstrap.min.css" rel="stylesheet" integrity="sha384-D/7uAka7uwterkSxa2LwZR7RJqH2X6jfmhkJ0vFPGUtPyBMF2WMq9S+f9Ik5jJu1" crossorigin="anonymous">
  </head>

  <body>
    
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">The Gossip Project</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <%= link_to "Team", team_index_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Contact", contact_index_path, class: "nav-link" %>
          </li>
          <% if current_user %>
          <li class="nav-item">
            <%= link_to "Messages", conversations_path, class: "nav-link" %>
          </li>
          <% end %>
        </ul>
        <ul class="navbar-nav my-2 my-lg-0">
          <% if !current_user%>
          <li class="nav-item">
            <%= link_to "Login", new_session_path, class: "btn btn-outline-primary mr-1"%>
          </li>
          
          <li class="nav-item">
            <%= link_to "Sign up", new_user_path, class: "btn btn-outline-primary" %>
          </li>
          <% else %>
            <li class="nav-item">
              <%= link_to "#{current_user.full_name}", user_path(current_user.id), class:"nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Logout", session_path(session[:user_id]), class: "btn btn-danger", method: :delete, data: { confirm: 'Es-tu sûr de vouloir vous logout?' }%> 
            </li>
          <% end %>
        </ul>
      </div>
    </nav>


<div class='text-center'>
  <div class='row'>
    <div class='col-md-2'>
      <!-- Left menu -->
      <!-- <div class="card">
          <div class="card-header">
            Villes
          </div>
          <ul class="list-group list-group-flush">
            <% City.all.each do |city| %>
              <li class="list-group-item"><%= link_to city.name, city_path(city.id), class:"nav-link" %></li>           
            <% end %>
          </ul>
      </div> -->
    </div>
    <div class='col-md-8'>
      <%= yield %>
    </div>
    <div class='col-md-2'>
      <!-- Right menu -->
      <!-- <div class="card">
          <div class="card-header">
            Utilisateurs
          </div>
          <ul class="list-group list-group-flush">
            <% User.all.each do |user| %>
              <li class="list-group-item"><%= link_to user.full_name, user_path(user.id), class:"nav-link" %></li>
            <% end %>
          </ul>
      </div> -->
    </div>
  </div>
</div>
    




  </body>
</html>
